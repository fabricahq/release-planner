---
name: release
description: Prepare a release pull request with drafted release notes. Use when asked to release, cut or issue a release, draft or revise release notes, or retry a failed release.
---
{{.Marker}}

# Prepare a release

This repository publishes releases with [Release Planner](https://github.com/fabricahq/release-planner) {{.Version}}. Print its release procedure and follow it:

```sh
go run {{.Module}}@{{.Version}} guide
```

Read `{{.NotesDir}}/README.md` first for this repository's release policy. You prepare the release pull request; the maintainer approves the release by merging it. Never tag, publish, or merge.
